@using Forge.CLI.Shared.Extensions
@using Forge.CLI.Shared.Helpers
@using System.Linq
@model Forge.CLI.Core.Templates.TemplateModel
using BAYSOFT.Abstractions.Infrastructures.Data;
using @(Model.Project.Name).Core.Domain.@(Model.ContextName).Interfaces.Infrastructures.Data;

namespace @(Model.Project.Name).Infrastructures.Data.@(Model.ContextName)
{
    public sealed class @(Model.ContextName)DbContextReader : Reader, I@(Model.ContextName)DbContextReader
    {
        public @(Model.ContextName)DbContextReader(@(Model.ContextName)DbContext context)
            : base(context)
        {
        }
    }
}