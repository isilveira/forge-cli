@using Forge.CLI.Shared.Extensions
@using Forge.CLI.Shared.Helpers
@using System.Linq
@model Forge.CLI.Core.Templates.TemplateModel
using System;
namespace @(Model.Project.Name).Core.Domain.@(Model.ContextName).Aggregates.@(RazorHelper.Pluralize(Model.EntityName)).Services
{
    public sealed class Delete@(Model.EntityName)ServiceRequest
    {
        public Delete@(Model.EntityName)ServiceRequest() { }
    }
}