@using Forge.CLI.Shared.Extensions
@using Forge.CLI.Shared.Helpers
@using System.Linq
@model Forge.CLI.Core.Templates.TemplateModel
using System;
namespace @(Model.Project.Name).Core.Application.@(Model.ContextName).Aggregates.@(RazorHelper.Pluralize(Model.EntityName)).Queries
{
    public sealed class Get@(Model.EntityName)By@(Model.Descriptor.Target.Name)Query
    {
        public Get@(Model.EntityName)By@(Model.Descriptor.Target.Name)Query() { }
    }
}