@using Forge.CLI.Shared.Extensions
@using Forge.CLI.Shared.Helpers
@using System.Linq
@model Forge.CLI.Core.Templates.TemplateModel
using System;
namespace @(Model.Project.Name).Core.Application.@(Model.ContextName).Aggregates.@(RazorHelper.Pluralize(Model.EntityName)).Commands
{
    public sealed class @(Model.Descriptor.Target.Name)@(Model.EntityName)Command
    {
        public @(Model.Descriptor.Target.Name)@(Model.EntityName)Command() { }
    }
}