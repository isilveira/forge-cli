artifact:
  id: Middleware.Configurations
  version: 1

layer: Middleware
type: Configurations
variant: null
description: "Generates a middleware Configurations"

model:
  source: metadata

generation:
  enabled: true
  target:
    namespacePattern: "{{projectConvention}}.Middleware"
    path: "src\\{{projectConvention}}.Middleware"
    filename: "Configurations.cs"
  template:
    engine: razor
    file: "Middleware/Configurations"

reading:
  enabled: false