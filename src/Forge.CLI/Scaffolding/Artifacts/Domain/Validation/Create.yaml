artifact:
  id: Domain.Validation.Create
  version: 1

layer: Domain
type: Validation
variant: Create
description: "Generates a domain validation Create"

model:
  source: metadata

generation:
  enabled: true
  target:
    namespacePattern: "{{projectConvention}}.Core.Domain.{{contextConvention}}.{{entityConvention}}.Validations.DomainValidations"
    path: "src\\{{projectConvention}}.Core.Domain\\{{contextConvention}}\\{{entityConvention}}\\Validations\\DomainValidations"
    filename: "Create{{entityName}}SpecificationsValidator.cs"
  template:
    engine: razor
    file: "Domain/Validation/Create"

reading:
  enabled: false