artifact:
  id: Domain.Validation.New
  version: 1

layer: Domain
type: Validation
variant: New
description: "Generates a New domain validation"

model:
  source: metadata

generation:
  enabled: true
  target:
    namespacePattern: "{{projectConvention}}.Core.Domain.{{contextConvention}}.{{entityConvention}}.Validations"
    path: "src\\{{projectConvention}}.Core.Domain\\{{contextConvention}}\\{{entityConvention}}\\Validations\\DomainValidations"
    filename: "Update{{name}}{{entityName}}SpecificationsValidator.cs"
  template:
    engine: razor
    file: "Domain/Validation/New"

reading:
  enabled: false