artifact:
  id: Domain.Validation.Delete
  version: 1

layer: Domain
type: Validation
variant: Delete
description: "Generates a domain validation Delete"

model:
  source: metadata

generation:
  enabled: true
  target:
    namespacePattern: "{{projectConvention}}.Core.Domain.{{contextConvention}}.{{entityConvention}}.Validations.DomainValidations"
    path: "src\\{{projectConvention}}.Core.Domain\\{{contextConvention}}\\{{entityConvention}}\\Validations\\DomainValidations"
    filename: "Delete{{entityName}}SpecificationsValidator.cs"
  template:
    engine: razor
    file: "Domain/Validation/Delete"

reading:
  enabled: false