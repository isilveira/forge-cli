artifact:
  id: Application.Query.GetById
  version: 1

layer: Application
type: Query
variant: GetById
description: "Generates a GetById application query"

model:
  source: metadata

generation:
  enabled: true
  target:
    namespacePattern: "{{projectConvention}}.Core.Application.{{contextConvention}}.{{entityConvention}}.Queries"
    path: "src\\{{projectConvention}}.Core.Application\\{{contextConvention}}\\{{entityConvention}}\\Queries"
    filename: "Get{{entityName}}ByIdQuery.cs"
  template:
    engine: razor
    file: "Application/Query/GetById"

reading:
  enabled: false