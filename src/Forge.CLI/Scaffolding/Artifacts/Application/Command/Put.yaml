artifact:
  id: Application.Command.Put
  version: 1

layer: Application
type: Command
variant: Put
description: "Generates a application command Put"

model:
  source: metadata

generation:
  enabled: true
  target:
    namespacePattern: "{{projectConvention}}.Core.Application.{{contextConvention}}.{{entityConvention}}.Commands"
    path: "src\\{{projectConvention}}.Core.Application\\{{contextConvention}}\\{{entityConvention}}\\Commands"
    filename: "Put{{entityName}}Command.cs"
  template:
    engine: razor
    file: "Application/Command/Put"

reading:
  enabled: false