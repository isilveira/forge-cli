artifact:
  id: Application.Notification.Put
  version: 1

layer: Application
type: Notification
variant: Put
description: "Generates a application notification Put"

model:
  source: metadata

generation:
  enabled: true
  target:
    namespacePattern: "{{projectConvention}}.Core.Application.{{contextConvention}}.{{entityConvention}}.Notifications"
    path: "src\\{{projectConvention}}.Core.Application\\{{contextConvention}}\\{{entityConvention}}\\Notifications"
    filename: "Put{{entityName}}Notification.cs"
  template:
    engine: razor
    file: "Application/Notification/Put"

reading:
  enabled: false